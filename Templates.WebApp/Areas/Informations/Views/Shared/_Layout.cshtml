@{ 
	Layout = "~/Views/Shared/_Layout.cshtml";
}

<partial name="Reusable/Header" />
<main>
    <div class="container-fluid">
        <div class="container">
            @RenderBody()
        </div>
    </div>
</main>
<partial name="Reusable/Footer" />
